# Campus Map App

A local React map app with markers, routes, notes, and map style controls.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:

   ```sh
   npm install
   ```

2. Start the dev server:

   ```sh
   npm run dev
   ```

3. Open:

   [http://localhost:3000/](http://localhost:3000/)
